*Link password to data
*Hash password with private key and store in file
*Password protect 'etc' folder with hash
*User must provide mathcing password to access program
*The key that was hashed with password used to decrypt data.
***If not password found, delete user data
*Someone erase password, data destroyed

*Program to check if folder is locked


1. Create and store password has in reg
2. lock folder/files with password hash
